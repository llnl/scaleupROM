# Everything the campaign produces or that is staged into the study at setup. The four
# tracked files -- TUNING.md, study.yaml, job.sbatch, corner2d.bootstrap.yml -- are the
# specification; the rest is run state.

# Written by the loop.
trials/
HISTORY.md
STOP

# Staged at setup, not authored here.
venv/
.agents/

# Produced by the solver. Restart files are shared across trials by design and can be large.
restart/
paraview/
metrics.h5

# Generated by setup_highrechannel.sh / gmsh2mfem.
meshes/

# The symlink to the built executable, which points outside the source tree.
main
